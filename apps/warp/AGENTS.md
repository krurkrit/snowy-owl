# Warp Adapter Agent Rules

- Read `APP.md` and `mapping.yaml` before editing.
- Use only Warp's supported YAML theme format.
- Do not force unsupported UI-font settings.
- Keep terminal contrast and JetBrains Mono guidance consistent with `APP.md`.
