# Codex Adapter Agent Rules

- Read `APP.md`, `mapping.yaml`, and `config.template.toml` before editing.
- Edit the template or mapping, never generated output.
- Preserve unrelated TOML keys, section order, and user settings during install.
- Treat unpublished Chrome appearance tables as unsupported.
