# Obsidian Adapter Agent Rules

- Read `APP.md` and `mapping.yaml` before editing.
- Use only Obsidian's supported CSS snippet mechanism.
- Require an explicit vault path; change only the Snowy Owl-owned snippet.
- Keep Inter, Noto Sans, and JetBrains Mono fallbacks in generated CSS.
