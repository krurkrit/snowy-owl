# Windows Terminal Adapter Agent Rules

- Read `APP.md` and `mapping.yaml` before editing.
- Patch only Snowy Owl-named schemes and explicitly owned defaults.
- Back up settings and preserve unrelated profiles, schemes, and keys.
- Keep the resulting `settings.json` valid after repeated installation.
