# Windows Terminal

`install.ps1 -App windows-terminal` backs up and patches the current Windows
Terminal `settings.json`, adds Snowy Owl Light/Dark schemes, selects them by
application light/dark mode, and sets JetBrains Mono in profile defaults. Review
`snowy-owl-schemes.json` for the generated values.
