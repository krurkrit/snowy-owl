# VS Code Adapter Agent Rules

- Read `APP.md` and `mapping.yaml` before editing.
- Use the official VS Code color-theme API; do not inject UI or font CSS.
- Keep font selection in user settings, outside the packaged color theme.
- Generate both themes and verify the packaged VSIX.
