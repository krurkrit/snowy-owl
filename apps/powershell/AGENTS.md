# PowerShell Adapter Agent Rules

- Read `APP.md` and `mapping.yaml` before editing.
- Use only colors supported by PSReadLine.
- Install inside the named Snowy Owl profile block.
- Preserve all profile content outside that owned block.
