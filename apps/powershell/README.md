# PowerShell

`install.ps1 -App powershell` adds a marked Snowy Owl PSReadLine block to
`$PROFILE`. It does not replace the rest of your profile. Re-running updates the
Snowy Owl block. `uninstall.ps1 -App powershell` removes only that block.
