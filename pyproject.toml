[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "snowy-owl-personalization"
version = "0.1.0"
description = "Portable, accessible personalization tokens and app adapters"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "Pillow==12.1.1",
  "PyYAML==6.0.3",
]

[tool.setuptools]
packages = []
